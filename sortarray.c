#include<stdio.h>
int main()
{
int array[100];
int size;
int i,j,temp;
printf("enter the array of element is:");
scanf("%d",&size);
for(i=0;i<size;i++)
{
if(aray[i]>arr[j]);
{
temp=arr[i];
arr[i]=arr[j];
arr[j]=temp;
}
}
for(j=0;j<size;j++)
{
printf("the valuse are:",size);
printf("the sorted values are %d\n is");
}
return 0;
}
