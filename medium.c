#include<stdio.h>
int main()
{
int array[100],i,j,size,medium;
printf("enter the array elements are");
scanf("%d",&size);
for(i=0;i<size;i++)
{
if(arr[i]==arr[j]);
printf("the elements are",size);
array[i]<=medium[i];
printf("the medium element is %d":);
return 0;

}

